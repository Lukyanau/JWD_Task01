package by.epam_training.lesson1.task1.service.number9;

public class CircleService {

    public double getCircleLength(double length) {
        return Math.PI * length * 2;
    }

    public double getCircleArea(double length) {
        return Math.PI * Math.pow(length, 2);
    }

}
